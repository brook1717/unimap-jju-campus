# Dijkstra / NetworkX / pgRouting logic will be implemented here in Week 2.
# The shortest path algorithm must execute via database-level geographic
# logic (PostGIS queries) or an in-memory NetworkX graph — never pure Python loops.
